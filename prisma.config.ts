// This file is reserved for Prisma v7+ config (defineConfig / datasource).
// The current project uses Prisma v5 — datasource URL is read from .env via schema.prisma.
// Keeping this file for future upgrade reference only.
export {};
